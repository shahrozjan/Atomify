import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Atomify Ethical Link Building Services",
  description: "ToDo",
};

export default function EthicalLinkBuildingServicesLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return <div>{children}</div>;
}
